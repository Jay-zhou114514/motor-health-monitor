# 暂停交接单（2026-09-22）

- 原因：用户提示**网络受限**，先暂停项目，回来后重开。
- 本文件的作用：**新会话读这一份即可继续**，不必依赖聊天记录。

## 0. 一句话状态

方法路线（阈值校准）已按预注册**判定终止**；论文定位已从"新方法"改为
**"覆盖率保证的层级依赖性 + 报告规范 + 受控负结果"**；
当前有**两个本地计算在跑**（不需要网络），另有一个审计任务已移交独立任务。

## 1. 正在运行（本地 CPU，不需要网络）

| 进程 | 内容 | 日志/检查点 |
| --- | --- | --- |
| `pid 3144` | **EXP-V3-01e**：修正后的 P2（每个测试实例的训练集 = 其余 23 个实例） | `outputs/exp_v3_01e_run.log`、检查点 `outputs/exp_v3_01e_partial.csv` |
| `pid 20124`（PowerShell 队列） | 串行执行：等 3144 结束 → **L3 重跑 EXP-V3-01d** → `l3_compare_v301d.py` 比对 → **EXP-V3-03**（M0–M5 覆盖率） | `outputs/CHAIN_v301e_l3_v303.log` |

**回来后的第一步**：查 `outputs/CHAIN_v301e_l3_v303.log` 末行是否 `chain finished`；
若要释放 CPU，可结束 3144 与 20124（不推荐，两者都在产出结果）。

## 2. 回来时要读的三份结果（跑完即有）

| 文件 | 回答什么 |
| --- | --- |
| `outputs/exp_v3_01e_summary.csv` | **H6**：P1 vs 正确 P2 的降幅差 ≤20 pp？ |
| `experiments/EXP-V3-01d-hashes.txt` | **L3**：EXP-V3-01d 是否逐字节复现（未过不得进入结果审查） |
| `outputs/exp_v3_03_coverage.csv` | **H11–H14**：Mondrian / weighted / localized / hierarchical / 自校准 的**单元级覆盖率**（含 Wilson CI） |

## 3. 已确立的判定（可辩护）

- **H5 不支持、H10 不支持**（批次④：λ 准则改为"覆盖约束下最小化 SD"、分辨率 620 窗 ≈0.16%、20 训练实例）：
  **阈值校准不降低跨单元 SD**，且移动阈值会让平均误报率从 1% 漂到 6.9–39.5%。
- **批次①②③部分结果已撤回**（准则缺陷 / P2 实现错误），撤回理由写在
  `experiments/EXP-V3-01d-verdict-addendum.md`。
- **H13/H14 待 EXP-V3-03** 决定最终措辞。

## 4. 必须遵守的措辞（用户 2026-09-22 修正，已采纳）

- **禁止**写"阈值校准救不了单元级不确定性"；
- 允许写：**"仅靠全局阈值校准，一般不能保证异质或发生分布漂移条件下的单元级条件覆盖率"**
  （Global threshold calibration alone does not generally guarantee unit-level conditional
  coverage under heterogeneous or shifted score distributions）；
- "口径依赖"统一改写为 **"覆盖率保证的层级依赖性"**（coverage guarantee depends on the level
  of conditioning），并显式区分 **marginal → group/unit-level → conditional** 三层。

## 5. 预注册清单（全部先于运行提交）

| 文件 | 内容 | 状态 |
| --- | --- | --- |
| `EXP-V3-01-preregistration.md` + 修订 1/2/3/4 | 单元级阈值校准（ULTC / 部分池化 / 修正准则） | 已判定（终止） |
| `EXP-V3-02-preregistration.md` | 信任判定器（元分析） | 已跑：γ CI 退化 → H7 不可主张；H8 不支持 |
| `EXP-V3-03-preregistration.md` | 覆盖层级（M0–M5） | **实现完成，队列中待跑** |
| `EXP-V3-04-preregistration.md` | 报告缺口审计 | **已移交新任务**（见 §6） |
| `EXP-V3-05-preregistration.md` | 多分析者实验 | 待用户招募参与者 |

## 6. 已移交出去的部分

- **EXP-V3-04 报告缺口审计**：已在新任务中执行（任务标题「EXP-V3-04 报告缺口审计（浏览器下载 30 篇 + 编码）」，
  threadId `01a0c961-962f-7ae2-8234-632821d66f1d`）。**本机对境外 API/站点网络受限**，
  该任务走用户已登录的浏览器会话；产物同样写回 `experiments/EXP-V3-04-*.csv`。

## 7. 未决事项（回来要定）

1. **投稿会场**：ICPHM（EI，1 月截止）/ IJPHM（Scopus，免费滚动）/ MDPI Sensors（SCI Q2，需 APC）/
   QR2MSE-ICRMS（EI，春季）。CCDC 已评估为最差匹配（25–35%）。
2. **EXP-V3-05 多分析者实验**：需要用户招 5–10 位参与者；材料（brief / 数据打包 / 提交模板 /
   分析脚本）尚未编写。
3. **可选增强**：扩独立单元（Ottawa 等，需下载）、消融 A4（记录预算）/ A6（特征集×跨轴承）。

## 8. 回来后的最小行动序列

1. 查队列日志 → 读 §2 的三份结果；
2. 更新 `EXP-V3-01d-verdict-addendum.md`（补 H6 与 L3 结论）；
3. 按 EXP-V3-03 的 H11–H14 定最终措辞（若某方法改善覆盖 → 撤回"缺乏支持"的强表述）；
4. 写 EXP-V3-05 的材料（brief + 打包 + 分析），并等用户招人；
5. 按"覆盖层级 + 缺口审计 + 多分析者"三块重写稿件，再按选定会场排版。
