# 项目交接与继续指南

最后更新：2026-09-17（EXP-V1-10 完成；门控仅剩 G4）

## 2026-09-17：EXP-V1-10 完成（Layer 3 由单点观察升级为量化结果）

**本轮先于结果提交的文件**

1. `docs/plans/V1_TO_V2_GATE.md`（v1.0）：转段判据 G1–G5、三层证据设计、版本纪律、限制清单；
2. `docs/FROZEN_PROTOCOL.md`（v1.0）：数据 / 特征 / 检测器 / 阈值 / 选择协议 / 报告格式冻结；
3. EXP-V1-10 修订 1（零模型 + 分数层指标 + 判定组合表）、修订 2（零模型计算规模）；
4. `src/exp_v1_10_selection_uncertainty.py`（可断点续跑）+ `src/exp_v1_10_extra_checks.py`（探索性）。

**EXP-V1-10 结果（Level B，单一试验台 + 固定 2 文件测试集）**

- 设计：固定测试集（IMS 1st_test 前 2 个文件，14 窗口），训练池 10 个文件，
  每次抽 7 fit / 3 validation；R = 50（另有 R = 200 精度检查与设计 B）。
- **选择准则几乎不判别**：86% 的复制中最小验证误报恰为 0，92% 的复制存在并列
  （≥2 个候选配置同分），最终配置由冻结的并列规则决定。
- **但报告的数字仍在动**：测试集完全固定的前提下，50 次重采样的测试误报率
  跨越 0%–14.29%，**SD = 4.13 pp**（均值 3.86%）；
  即使在 44 个都选中同一配置的复制内部，SD 仍为 3.58 pp。
- 连续尺度一致（test_pct_mean SD = 2.53 pp）；设计 B（测试集也重采样）SD = 4.56 pp。
- 零模型：观察到的模态占比（0.88）**高于** N1 非参零模型上界（0.805），方向为"更集中"；
  测试误报 SD（0.0413）落在 N1 区间 [0.016, 0.059] 内。
  → **没有证据**表明选择不稳定超出有限样本噪声。
- 探索性（Level C）：同样 50 个划分上，3σ RMS 的测试误报恒为 0；
  默认 iForest(200, 0.8) 不搜索时的波动（SD 3.34 pp）与调参后基本相同。
- 参考值：3σ RMS = 0.0000；默认 iForest = 0.0714（同一固定测试集）。

**结论（论文可用口径）**

> 在 n ≈ 10 个健康记录时，报告的误报率主要取决于"哪几条记录进了训练集"，
> 而不是"选了哪个超参"。单次划分上报告的误报率是**划分与样本量**的属性，
> 不是**方法与调参**的属性。

**下一次继续（当前唯一阻塞）**

- **G4**：核实 Paderborn 数据的独立单元定义（物理轴承 vs 记录）、许可与获取方式，
  并写出 V2-01 预注册；完成后转入 **V2-01（跨体系复现）**。
- V1 **不再新增实验**（参见 `V1_TO_V2_GATE.md` §2 的转段规则）。
- 若确需重跑：`D:\Python\python.exe src\exp_v1_10_selection_uncertainty.py --r-primary 50 --r-large 200 --null-datasets 40 --null-r 20 --ncurve-datasets 5 --ncurve-r 15`
  （已完成实验臂会自动复用；`--force` 全量重算）。
## 2026-09-17：文献审计 + 发布 V1.6（三层可靠性框架）

- 针对性文献审计（Scopus）：stability selection 463 / nested CV 2,064 / HPO variance 369 /
  model-selection uncertainty 599 → **"选择不确定性"概念非原创**；
  但 small-sample + one-class + eval/hyperparam（24）与 evaluation protocol + FD/CM（27）
  中**无一篇属于本领域的选择不确定性研究**。
  → 贡献定位：**成熟方法学的领域应用 + 量化案例 + 报告规范**。
- 发布 `docs/plans/RESEARCH_PLAN_v1.6.md`：
  - 统一研究问题："小样本、仅健康数据的轴承异常检测实验，结论有多可靠？"
  - 三层框架：数据/划分（V1-05→07）、模型选择（V1-08→09）、结果报告（新增）；
  - 五维评价：Performance / Selection stability / Selection cost / Selection sensitivity / Generalization；
  - 原则：**不再增加模型**。
- 措辞修正（按审查意见）：OC-SVM 限定在小样本 normal-only RBF 设定与预定义协议；
  报告措辞改为"只报告选择后单一性能，可能掩盖选择过程的不确定性"。
- 新增 `experiments/EXP-V1-10-preregistration.md`：固定测试集 + 50 次训练/验证重采样，
  量化选择稳定性与测试表现波动（含 7.1% 分辨率限制的预先声明）。

## 2026-09-17：EXP-V1-09 训练内超参协议

- 协议：嵌套 leave-one-file-out（内层选参、外层评估），**测试文件全程不参与选参**；超参网格预注册冻结。
- 结果（批次均值 FP rate，判定阈值 5% 为**预先设定的判据**、非方法应有水平）：
  - **C One-Class SVM（调参后）：0/3 达标**（13.1% / 35.7% / 26.2%）；
  - **D Isolation Forest（调参后）：3/3 达标**（1.2% / 4.8% / 2.4%）；
  - 参考：A 3σ RMS 三批次均为 2.38%；B 马氏 4.8% / 11.9% / 16.7%。
- 成本：D 每折 6.4–14.0 秒（含内层搜索），A 为 0.0001 秒（约 10⁵ 倍差距）。
- P4（选参稳定性）：成立——1st_test 的 iForest 在 12 折中仅 33.3% 选到同一组合。
- 判定：**"超参没调好"对 iForest 成立、对 OC-SVM 不成立**（后者是结构性失败）；
  现代方法的"达标"伴随极高搜索成本与不稳定的选参过程。
- 下一步：结果审查 → 发布 V1.6（把"选择过程的不确定性"写入协议），
  并评估是否进入 EXP-V1-10（记录级 ROC/误报预算协议）或 EXP-V1-11（Paderborn 独立轴承）。

## 2026-09-17 晚间：发布 V1.5 / 双线 V1.4

- EXP-V1-08 结果审查完成：**P1 不成立**（现代单类方法误报不更少）、P2 成立但退化、**P3 成立**（无普适最优方法）。
- 假说判定：**"现代单类方法更可靠"被否定**；"简单方法在记录级误报控制上有竞争力"得到支持（Level B）。
- 风险审查：每折仅 11 个训练窗口；OC-SVM 仅一组超参；独立单元 3+1；MFPT 判别过易；无深度/真实数据。
- 已发布：`docs/plans/RESEARCH_PLAN_v1.5.md`（主线：评价协议优先于模型复杂度）；
  会议线 v1.4（投稿门槛评估：已基本满足）；期刊线 v1.4（扩展路线）。
- 新增 `docs/AI_USE_DISCLOSURE.md`：如实披露 AI 辅助范围与人（项目负责人）的责任边界；
  **所有日期与提交历史保持原样，不做修改或回填。**
- 下一实验：EXP-V1-09（训练内超参协议），用于回应"是不是超参没调好"的质疑。

## 2026-09-17：文献精读 + EXP-V1-08 基线实验

**文献（已读全文）**
- Vieira et al. (2026, MSSP, CC-BY, arXiv:2509.22267)：数据泄漏审计 + bearing-wise 划分 + 多标签 + ROC 指标 + 轴承数量影响；四数据集（CWRU/Paderborn/Ottawa/HUST）。
  关键词检索：`unsupervised` / `one-class` / `anomaly detection` / `statistical power` / `sample size` **全文 0 命中**。
- Knap et al. (2026, PHME)：CWRU+Paderborn 的 6 个固定跨域场景 + recording-level 泄漏安全评估（已读全文 8 页）。
- 结论：**"泄漏安全划分"已是既有工作，不能作为我们的创新点**；增量收窄为
  **无监督/仅健康数据 + 统计功效 + 公开报告不复现实例**。
- 合法获取情况：PHME 与 Vieira（arXiv/CC-BY）已下载全文；Qian 2023（MSSP）仍无开放版本。

**EXP-V1-08 结果（预注册提交 9807c4e，先于结果）**
- 四种检测器只用健康窗口训练，统一 99% 分位报警线。
- MFPT 上四者 AUROC 全为 1.000（判别力无差异）；差异全在**健康记录误报**。
- IMS 三批次误报率：A 3σ RMS **2.4% / 2.4% / 2.4%**（最稳）；D iForest 3.6–4.8%；
  B 马氏 4.8–16.7%；**C One-Class SVM 27.4–50.0%（最差）**。
- 预注册判据：P1 **不成立**（现代单类方法误报不更少）、P2 成立但退化（全员召回 1.000）、P3 **成立**（无普适最优方法）。
- 解释：小样本下单类方法对训练量高度敏感——**方法复杂度不能替代样本量**。

**下一步**：结果审查 → 假说判定 → 风险审查 → 发布研究计划 V1.5 与双线 V1.4（主线：评价协议优先于模型复杂度）。

## 2026-09-16（EXP-V1-07 完成）

## 2026-09-16 晚间：EXP-V1-07 结果

- 数据：IMS 1st（12 健康）+ 2nd（6）+ 4th（6）= 24 个健康文件，3 个独立批次；MFPT 3 个正常文件。
- 预注册判据结果：**P1 不成立**（IMS 上问题组膨胀比 > 1 的折占比 0/24）；
  P2/P3 形式成立（收缩都把膨胀比降低了），但属退化性结果。
- 关键结论：EXP-V1-05/06 观察到的"距离膨胀导致大量误报"**未在三个独立批次上复现**；
  MFPT 上的 1.576 只出现在"留出 baseline_3"这一单一折叠。
- 影响：H2 的"修复"叙事不成立（无膨胀可修）；机制结论降级为"单数据集单文件对个案"。
- 建议主线改为**负结果 + 评价方法学**：单数据集上看似显著的失效模式，跨批次不复现；
  报告此类机制前必须做跨批次/跨设备验证与功效分析。
- 文档地址：`experiments/EXP-V1-07-cross-dataset.md`、`experiments/EXP-V1-07-run-log.txt`、
  `experiments/EXP-V1-07-*.csv`、`docs/figures/exp_v1_07_cross_dataset.png`。

## 2026-09-16 进度快照

## 2026-09-16 进度快照（下次从这里继续）

- 完成 EXP-V1-06 的**确定性证据预检**（`experiments/EXP-V1-06-evidence-check.md`）：
  21/21 个引用数字与结果文件一致，P1–P3 / Q1–Q3 独立重算完全一致，无幻觉证据。
- 完成 **V1.4 功效分析**（`docs/plans/POWER_ANALYSIS_V1.4.md`，蒙特卡洛 + 敏感性分析）：
  H1 实测 dz≈0.55 → 需约 29 个正常文件；H2 实测 dz≈1.01 → 需约 10 个；
  当前 3 个正常文件下功效仅 0.087（H1）与 0.180（H2）。
- **重要修正**：H1 的阴性结果应表述为"功效不足、无法判定"，而不是"未复现/被反驳"。
- 发布 `RESEARCH_PLAN_v1.4.md`：主线转为 H2（正则化与选择准则），下一实验 EXP-V1-07
  （H2 跨数据集验证，≥10 正常文件）。
- 说明：`result-to-claim` / `experiment-audit` / `research-review` 这三个 skill 依赖外部评审后端
  （MCP reviewer），本机不可用，因此判决步骤按 skill 规定 fail closed，
  改由人工判定 + 确定性证据预检承担。

## 2026-09-15 进度快照（下次从这里继续）

- 建立固定的研究计划版本管理：`docs/plans/README.md`
  （实验完成 → 分析结果 → 更新计划版本 → 决定下一实验）。
- 归档 V1.0（`docs/plans/RESEARCH_PLAN_v1.0.md`），发布 V1.1（`docs/plans/RESEARCH_PLAN_v1.1.md`）。
- V1.0 → V1.1 逐条变更（保留/删除/修改/新增/延后/因结果改变）：`docs/plans/CHANGELOG.md`。
- 统一实验编号（以仓库登记为准）：EXP-V1-00 基线流水线、EXP-V1-01 阈值敏感性、
  EXP-V1-02 特征消融、EXP-V1-03 误差定位、EXP-V1-04 良态特征集受控对照（下一实验）。
- 当前阶段：Failure Mechanism Investigation；下一实验：EXP-V1-04。
- 完成 EXP-V1-04 良态特征集受控对照（记录 experiments/EXP-V1-04-conditioning-control.md）：
  预测 1（条件数越高误报越多）部分支持（描述性 rho=0.745，但 11 个特征集非独立、
  Bonferroni 校正后不显著）；预测 2（条件数越高阈值越不稳）不支持（rho=0.264, p=0.43）。
  误报只出现在含 RMS+谱质心的组合，其距离膨胀比 > 1（1.35~1.50），其余特征集 < 0.8。
  机制表述修正为：当前结果提示误报可能与 RMS–谱质心近共线结构及其低方差方向与正常文件间分布差异的对齐有关（合理假说，尚未证实）。项目负责人最终审查结论已写入 EXP-V1-04 记录第 7 节。
- 完成 EXP-V1-05 Covariance Geometry & Regularization（记录 experiments/EXP-V1-05-covariance-geometry.md）：
  方向归因——问题组 93.7% 的 D² 来自最小特征值方向（标准化投影 2.93），但对照组也表现为对齐
  （88.8%、1.61），故 H1 部分支持；正则化——LW δ=0.220（训练解析式）使问题组
  FP 9→1、膨胀比 1.58→0.31、F1 0.873→0.984，反例组不受影响，H2 部分支持。
  新暴露的问题：LOO 似然选择 δ=0（无效），"不用测试数据如何选择收缩强度"尚未解决。
- 项目负责人已完成 EXP-V1-05 结果审查，判定为 **B**：保留 H1/H2 为 Level-B 工作假说，
  先补证据、先预注册判据，再决定是否进入"几何归因 + 修复"叙事（不选 C：机制未被推翻；
  不选 A：关键证据链仍有缺口）。
- 已发布新版计划：Master `docs/plans/RESEARCH_PLAN_v1.3.md`；
  双线 `conference-track/RESEARCH_PLAN_v1.2.md`、`journal-track/RESEARCH_PLAN_v1.2.md`。
- 完成 EXP-V1-06 前瞻性验证（预注册提交 2aa165c；记录 experiments/EXP-V1-06-prospective-validation.md）：
  按预注册判据，P1 2/3 折成立（F2 差距极小）、P2 仅 1/3 不成立、P3 成立 → H1 总体"部分支持"；
  且问题组的"膨胀 + 大量误报"只在 F1（留出 baseline_3）复现，H1 复现性不足（降为 Level C）；
  Q1/Q2/Q3 全部成立 → H2 获三折支持（Level B+），其中简单准则 C5（条件数 ≤ 100 的最小 δ）稳定有效，
  而基于似然的 C2/C4 准则在 F1 完全无效。
- 待办：EXP-V1-06 结果审查 → 假说判定 → 风险审查 → 更新研究路线 → 发布新版计划。
- 论文故事（当前版本）：简单可解释的异常检测在严格评估 + 小样本 + 高相关特征下的
  失效机制与稳健化策略。

## 2026-09-14 进度快照（下次从这里继续）

- 完成 EXP-V1-01 阈值敏感性分析（脚本 `src/exp_threshold_sensitivity.py`，
  记录 `experiments/EXP-V1-01-threshold-sensitivity.md`）。
- 结果：方法 A 在 k=2.5~4 完全稳定（F1=1.000），仅 k=2 出现 1 个误报；
  方法 B 在分位数 0.90~0.999 之间指标完全不变（F1=0.873，FP=9）。
- 关键发现：方法 B 的误报不是阈值问题，而是两个正常文件之间存在分布差异
  （训练正常距离 p95=2.80；测试正常文件距离 2.37~4.74，全部越线）。
- 完成 EXP-V1-02 特征消融（记录 experiments/EXP-V1-02-feature-ablation.md）：所有特征集召回率 1.000，差异全在误报；RMS only 误报 1，加入谱质心后 8，四特征 9。证据：单变量偏移均小于 0.7 sigma，但相关性结构差异大（rms-kurtosis 从 -0.065 变为 +0.511）。当前结果与多变量依赖结构变化的解释一致，但样本量小（22 训练窗口、1 个正常测试文件），不能作为已证实结论，需 EXP-V1-03 的 bootstrap 置信区间检验。
- 完成 EXP-V1-03 误差定位与依赖结构验证（记录 experiments/EXP-V1-03-error-localization.md）：
  误报分散于整段记录（9/11 窗口）；四个关键相关性的 bootstrap 95% 置信区间全部重叠，
  因此不能宣布"相关性反转"；leave-one-feature-out 显示删掉 RMS 或谱质心后误报从 9 降到 2~3；
  四特征协方差条件数达 3.8e9（训练）/2.0e10（baseline_3），而 rms+kurtosis 仅 1.8/3.1。
  证据提示：病态协方差（近共线特征 + 小样本）是比"依赖结构真实变化"更受支持的机制，
  但仍需更多正常数据确认。
- 下一步：EXP-V1-04 良态特征集对照实验（检验"降低条件数即可减少误报"这一可检验预测），
  之后视结果决定是否引入收缩协方差（EXP-V1-05）。

## 2026-09-13 进度快照

对应《阶段性科研计划》阶段 1（V1 正确性审计）开头。

- P0 修复：`src/plotting.py` 的 False Alarm / Missed Detection 变量名写反，已改正；
  两个检测器的 `predict` 改为索引安全（存在缺失值时不再错位）。
- P0 数据泄漏检查：主结果阈值只由训练正常数据确定，无泄漏；
  成本分析原先在测试集上选最优阈值（事后最优），已新增 `src/validation.py`
  的 leave-one-file-out 泄漏安全版本。发现：LOFO 结果与方法 B 原结果一致
  （F1=0.873），说明本次没有虚高，但流程风险已修正。
- P1 多层评价：新增 `src/multi_level.py` 与报告 4.2 节。文件级结果：
  方法 A 5/5 故障文件检出、正常文件 0/1 误报；
  方法 B 5/5 检出、正常文件 1/1 误报（FPR=0.818）。
- 指标补齐：FPR、FNR、Specificity。
- 测试：22 个用例全部通过（`python -m pytest -q`）。
- 下一步（阶段 2）：Threshold Sensitivity（2/2.5/3/3.5/4σ；95/97.5/99/99.5/99.9%）
  → Feature Ablation → Error Analysis → 整理 V1 final report。

## 2026-09-11 进度快照

- 新增成本敏感阈值选择 `src/cost_analysis.py`：在"漏报:误报 = N 倍"代价假设下
  扫描报警线、选总代价最低的阈值；已集成进 `run_pipeline.py`
  （报告第 5 节、`outputs/figures/cost_curves.png`）。
- 发现：本数据集两类分得很开，方法 A 最优阈值在 1～50 倍代价假设下都稳定在 ~2.2σ；
  成本分析的价值要等更难/分布漂移的数据（v2 跨数据集）才能体现。
- 新增 `tests/` 单元测试（16 个用例，`python -m pytest tests`），全部通过。
- README / ROADMAP 已同步更新（成本视角章节 + 测试说明）。
- 下一步：v2.1 跨数据集泛化验证（MFPT 训练正常状态 → NASA IMS 测试泛化能力）。

## 2026-09-10 进度快照

- v1 已完成并在真实公开数据上跑通：8 个文件、训练 22 个正常窗口、测试 42 个窗口（31 个故障）。
- 结果：方法 A（3σ RMS 阈值）F1 = 1.000；方法 B（马氏距离）召回率 1.000、精确率 0.775（误报偏多）。
- 已生成 `outputs/report.md` 与 `docs/figures/` 结果图；README 已写成作品介绍。
- 已完成：创建并推送 GitHub 新仓库 motor-health-monitor（2026-09-10）。
- 下一步：v2 跨数据集泛化验证（MFPT → NASA IMS）。
## 学生目标与背景

- 自动化专业，本科大三，预计 2028 年毕业。
- GPA 3.1/5.0；英语六级 529。
- 目标：通过持续、可验证的项目成果提升竞争力，准备申请港科大（广州）红鸟挑战营及红鸟硕士项目。
- 当前资源：电脑、每天至少 2 小时，周末时间更多；暂无实验室、硬件、竞赛或科研基础。

## 选定方向

主线是“面向小型工厂的低成本电机健康监测与维护决策系统”。它对应自动化、传感器、信号处理、嵌入式与智能工业化。

当前处于**零硬件阶段**：先用模拟/公开振动数据完成信号处理与异常检测，再逐步补充真实数据、验证报告和低成本硬件。

## 已完成

- GitHub 仓库：<https://github.com/Jay-zhou114514/motor-health-monitor>（v1 已完成并上传，2026-09-10）
- 本地项目目录：`C:\Users\32597\Documents\Codex\2026-09-06\github\motor-health-monitor`
- Git 已配置、远程 `origin` 已关联、项目已成功推送到 `main` 分支。
- 已创建并运行第一个 Python 原型：
  - `src/analyze_signal.py`：生成模拟电机振动信号；以“偏离均值超过 2 个标准差”为规则标记异常；输出图表和报告。
  - `README.md`：项目介绍与运行方法。
  - `.gitignore`：忽略生成的数据与输出文件。

## 如何运行

在项目文件夹打开 PowerShell，运行：

```powershell
python src/analyze_signal.py
```

程序会生成：

- `data/sample_vibration.csv`
- `outputs/signal_plot.svg`
- `outputs/report.md`

## 下一步：第一节学习任务

1. 在项目文件夹终端运行 `code .` 打开 VS Code。
2. 打开 `src/analyze_signal.py`。
3. 找到 `for index in range(600):`。
4. 将 `600` 改为 `1000`，保存。
5. 在 VS Code 终端运行 `python src/analyze_signal.py`，观察报告中样本数的变化。
6. 下一次继续讲解：循环、正弦波、随机噪声，以及为什么用均值和标准差识别异常。

## Git 基础命令

每次完成一个小功能后，在项目目录中执行：

```powershell
git add .
git commit -m "描述这次修改"
git push
```

不要使用 `git push --force`，除非明确理解它会覆盖远程历史。

## 中长期路线

1. 用公开轴承/电机振动数据替换模拟数据。
2. 比较多个方法：阈值法、特征工程分类、轻量异常检测。
3. 写实验记录：数据来源、方法、结果、局限性。
4. 找老师/实验室或竞赛队时，用已完成的原型和报告展示能力。
5. 在有条件后，用 ESP32、振动、温度和电流传感器搭建低成本验证台。

## 红鸟挑战营准备原则

材料与面试不能只展示技术。要保留“真实需求—问题定义—多个方案—原型—测试—风险与改进”的证据，并持续练习团队协作、表达、系统思维与英文项目介绍。




















