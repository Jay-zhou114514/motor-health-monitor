# EXP-V2-06：在 run-to-failure 数据集上补齐第二/第三个检测器

- 日期：2026-09-19
- 状态：已完成（**待结果审查**）
- 预注册：`EXP-V2-06-preregistration.md`
- 脚本：`src/exp_v2_06_multi_detector.py`
- 结果：`EXP-V2-06-raw.csv`、`-summary.csv`、`-run-log.txt`

## 1. 判定汇总

| 检测器 | Z1（SD 随 k 下降） | Z2（SD(k_max) < SD(k=1)） | 判定 |
| --- | ---: | ---: | --- |
| **A 3σ RMS** | **6/6** | **6/6** | 成立 |
| D Isolation Forest（EXP-V2-05） | 6/6 | 6/6 | 成立 |
| **B 马氏距离** | **2/6** | **2/6** | **不成立** |
| **Z3（三者方向一致）** | — | — | **不成立** |

## 2. 马氏距离为何不成立：**k=1 处饱和**

| 检测器 | k=1 时的均值误报率 | k=4 时 |
| --- | ---: | ---: |
| A 3σ RMS | 43–50% | 12–33% |
| **B 马氏距离** | **67–100%** | 27–50% |

其中 **XJTU-SY / 35Hz12kN / k=1 的均值误报 = 100.0%** → **SD = 0（退化格，L1 已捕获）**。

后果：**"SD 是否随 k 下降"这一判据在马氏距离上从被污染的基线出发**——
k=1 已经是 67–100% 的饱和水平，"下降"没有意义。
（3σ RMS 在同一工况 k=1 仅 49.5%，未饱和，故判据有效。）

## 3. 可复现性验证（①–③）

| 层 | 结果 |
| --- | --- |
| ① L1 | ✅ 通过；**显式捕获 1 个退化格**（马氏 / XJTU-SY / 35Hz12kN / k=1，mean FP = 1.00） |
| ② L2 | ✅ mean/SD 逐项一致 |
| ③ L3 | ✅ **逐字节一致**（raw `0432bf4b…`、summary `9fe28efd…`） |

## 4. Interpretation

1. **单元效应在两个检测器上成立、在第三个上无法检验**：
   3σ RMS（6/6）与 Isolation Forest（6/6）都支持"独立单元数降低不确定性"；
   马氏距离因 **k=1 处饱和** 而**不在该主张的适用范围内**。
2. **反过来，马氏距离本身是一个可报告的结果**：
   在 q=0.99、跨轴承（k=1）条件下，它对**另一颗健康轴承**的误报率达 **67–100%**，
   即该配置下的马氏检测器实质上不可用。这与 EXP-V1-08 中"马氏误报 4.8–16.7%"
   并不矛盾——那是 **within-bearing** 口径，本实验是 **bearing-level**（口径效应）。
3. **判据缺陷复发（必须记录）**：反模式清单第 7 条正是"判定规则未考虑退化/饱和"，
   我在预注册时**列出了它却没有实现对它的处理**，导致 Z1/Z2 在马氏距离上被污染基线干扰。
   这是同一类错误的**第二次**发生（前一次是 EXP-V2-03 的 P1）。

## 5. Limitation

1. 三个检测器同属"阈值取训练分数分位"的范式，不得声称对任意检测器成立；
2. 马氏距离的结论受饱和限制，**只报告"无法检验"，不报告"效应不存在"**；
3. 仍只有两个 run-to-failure 数据集、6 个工况单元；
4. 未对饱和设定显式阈值（如"k=1 均值误报 > 90% 即判为不可检验"）——**下一版预注册必须加入**。

## 6. 对论文的含义

第二层的表述必须写成：

> 在**未饱和**的检测器上（3σ RMS、Isolation Forest），
> 独立单元数降低报告误报率的不确定性；
> 马氏距离在跨轴承设置下饱和（67–100% 误报），**不在该主张范围内**。
---

## 7. ④ 结果审查（逐条对照预注册判据）

| 预注册判据 | 要求 | 实际 | 判定 |
| --- | --- | --- | --- |
| Z1 | A、B 各自"SD 随 k 下降"占比 ≥ 80% | A **6/6**；B **2/6** | A 成立、B 不成立 |
| Z2 | A、B 各自 SD(k_max) < SD(k=1) ≥ 80% | A **6/6**；B **2/6** | 同上 |
| Z3 | 三者方向一致 | **不一致** | **不成立** |
| Z4 | 报告 k=1 均值误报水平 | A 43–50%；B **67–100%** | 已报告 |

## 8. ⑤ 假说判定

> **判定：部分支持（Level B）。**
> "工况固定下，独立单元数降低报告误报率的不确定性"在 **3σ RMS 与 Isolation Forest 上成立（各 6/6）**；
> **马氏距离因 k=1 处饱和（误报 67–100%，其中一个单元达 100% → SD=0）而不具备可检验性**。
> 因此该主张的适用边界是**未饱和的检测器**，不是"任意检测器"。

## 9. ⑥ 风险审查

**判据缺陷复发（第二次）**：反模式第 7 条"判定规则未考虑退化/饱和"已被列入清单，
但**预注册未实现其处理方式**，导致 B 的 Z1/Z2 从被污染的基线出发。
→ 已补反模式第 11 条：**预注册必须写明每条风险"如何落实"，而不只是列出风险名称**。

**我在上一版回答里过强的话**：

| 我说过 | 更正 |
| --- | --- |
| "第二层现在以主张 A 表述，已干净；'只有一个检测器'成为剩余最大短板" | 补检测器后**出现了新边界**：主张 A 只在**未饱和**检测器上成立。"干净"应改为"**边界已明确**" |
| 隐含预期"三个检测器应一致" | 这正是**未预先定义"饱和如何判定"**的后果；不应期待一致，而应预先定义可检验性条件 |

**替代解释**：马氏距离的失败也可能来自"经验协方差在 4 维、20 条记录上估计不稳"（n≈20 < 4 维的可靠估计），
而非仅"跨轴承饱和"。两者都指向同一结论（该配置不可用），但归因不同，**不得单方面断言**。

## 10. ⑦ 下一实验（路线更新）

**建议：暂停新增实验，转入论文写作。** 理由：

1. 三层证据已完整且边界清楚：
   - **口径**：within-bearing 0.00% → bearing-level holdout 40.63%（EXP-V2-03）
   - **单元数**：工况固定下，3σ RMS 与 iForest 各 6/6（EXP-V2-05/06）；马氏距离**饱和、不在范围内**
   - **定义**：健康阶段规则极差 2.40 / 5.19 pp（EXP-V2-04），其中 PRONOSTIA 为干净测量
2. 剩余可选加强项（主张 B 的固定-k 检验、分位数 q 敏感性）**不影响现有主张的正确性**，
   可作为审稿意见的备选回应，而非投稿前置条件；
3. 继续加实验的边际收益已低于"把已有结果写清楚"的收益。

**备选回应清单（若审稿人提出）**：
- "只有两个数据集" → 退路：Paderborn + IMS 支撑口径与单元数两层
- "只有一个检测器成立吗" → 3σ RMS 与 iForest 两个；马氏距离饱和，已如实报告
- "健康阶段规则人为" → 敏感性扫描本身即贡献

## 11. ⑧ 版本同步

- 实验注册表：已登记 EXP-V2-06
- CHANGELOG：已记录 V2-06 结果与判据缺陷
- 计划 v1.7 §二：第二层表述改为"**未饱和检测器上成立**"
- 门控：已声明其职责移交"投稿就绪度"，本次不再变更